[remote "origin"]
    prune = true
[core]
    editor = notepad
[alias]
    st = status
    co = checkout
    df = diff
    lg = log -p
    p = push
    c = commit --verbose -n -m
    cm = commit -a --verbose -n
    conflict = diff --name-only --diff-filter=U
